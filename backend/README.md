# Contact Form Backend

This backend handles contact form submissions and saves them to a JSON file.

## Setup Instructions

1. Install Node.js (if not already installed)

2. Navigate to the backend folder:
```bash
cd backend